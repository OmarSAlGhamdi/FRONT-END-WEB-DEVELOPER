var flavor = "vanilla";
var vessel = "cone";
var toppings = "peanuts";

if ((flavor === "vanilla" || flavor === "chocolate") && (vessel === "cone" || vessel === "bowl") && (toppings === "sprinkls" || toppings === "peanuts")) {
    console.log("I'd like two cups of " + flavor + " ice cream in a " + vessel + " with " + toppings);
}